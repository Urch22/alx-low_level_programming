#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char n;
	int m;

	n = 'a';
	m = 0;
	while (m < 10; m++)
	{
		putchar(m + 0);
	}
	for (n <= 'f'; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
