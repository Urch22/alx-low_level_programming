#include "main.h"
/**
 * _isdigit - checks for a digit (0 through 9)
 * @c: Function parameter
 * Return: 1 if c is a digit, 0 if it is not
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
